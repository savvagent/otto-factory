.svelte-kit
.wrangler
build
node_modules
package-lock.json
src/lib/paraglide
project.inlang/cache
# Generated by `paraglide-js compile` alongside the cache dir above, not
# committed (see the root .gitignore) — `npm run check` runs the compiler
# before `npm run lint` does its `prettier --check .`, so these must be
# excluded here or a fresh checkout fails lint on files nobody wrote.
project.inlang/.meta.json
project.inlang/.gitignore
project.inlang/README.md
